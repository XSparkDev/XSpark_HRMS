"use client"

// This page is an alias for the main Settings page.
// Supervisors are routed here via navigation, but the UI is the same as /settings.

import SettingsPage from "../page" // app/settings/page

export default function SupervisorSettingsAliasPage() {
  return <SettingsPage />
}


